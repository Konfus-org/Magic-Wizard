<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <!-- Source-code form of the DryIoc NuGet package: compiled straight into
         Core.dll, so there is no DryIoc.dll for a gem to ship its own copy of.
         PrivateAssets="all" stops the source files flowing into gem projects,
         which would compile a second, conflicting copy of every DryIoc type. -->
    <PackageReference Include="DryIoc" Version="5.4.3" PrivateAssets="all" />
  </ItemGroup>

</Project>
